# راه‌اندازی هماهنگ‌سازی دبیرخانه

این نسخه ساختار اولیه و عملیاتی هماهنگ‌سازی LAN را اضافه می‌کند:

- هر دستگاه SQLite محلی خودش را دارد.
- تغییرات رکوردها به صورت delta در `sync_changes` ثبت می‌شوند.
- فایل‌ها با SHA-256 مقایسه می‌شوند و فقط فایل‌های متفاوت منتقل می‌شوند.
- یک دستگاه نقش «اصلی / مادر» دارد و شماره نامه‌های جدید را رزرو می‌کند.
- دستگاه دوم برای شماره جدید از مادر شماره می‌گیرد.
- ارتباط با کلید مشترک انجام می‌شود و خود کلید روی شبکه ارسال نمی‌شود؛ درخواست‌ها با HMAC امضا می‌شوند و timestamp دارند.
- آیکون بکاپ در HomePage در حالت هماهنگ سبز، هنگام انتقال نارنجی و در حالت قطع بدون تغییر رنگ نمایش داده می‌شود.
- تنظیمات در تب «هماهنگ‌سازی» داخل دیالوگ پشتیبان‌گیری قرار گرفته است.

## وابستگی جدید

در `pubspec.yaml` این dependency را اضافه کنید:

```yaml
dependencies:
  crypto: ^3.0.6
```

سپس:

```bash
flutter pub get
```

## Android و شبکه داخلی

در صورتی که برنامه Android با HTTP داخلی کار می‌کند، Android باید cleartext traffic را برای این ارتباط اجازه دهد. در `android/app/src/main/AndroidManifest.xml` در application این ویژگی را اضافه کنید:

```xml
android:usesCleartextTraffic="true"
```

مجوز معمول شبکه نیز باید وجود داشته باشد:

```xml
<uses-permission android:name="android.permission.INTERNET" />
```

## جفت‌سازی

1. روی دستگاه مادر، نقش را روی «اصلی / مادر» بگذارید و هماهنگ‌سازی را فعال کنید.
2. کد اتصال نمایش داده‌شده را Copy کنید.
3. روی دستگاه دوم، نقش «دستگاه دوم / متصل» را انتخاب کنید.
4. کد را در «کد جفت‌سازی مادر» Paste و «اعمال کد و اتصال» را بزنید.
5. بعد از اتصال، هر دو دستگاه باید وضعیت سبز را نشان دهند.

فرمت کد:

`DABIRKHANE-SYNC|IP|PORT|KEY`

## نکته امنیتی

کلید خام در header ارسال نمی‌شود. هر درخواست با HMAC-SHA256 و timestamp امضا می‌شود. بنابراین شنود ساده شبکه، کلید را در اختیار مهاجم قرار نمی‌دهد.

برای اینترنت عمومی، مرحله بعد باید HTTPS/TLS و احراز هویت سرور اضافه شود؛ این نسخه برای LAN طراحی شده است.

## محدودیت این مرحله

در این مرحله جفت‌سازی با «کد امن قابل Copy/Paste» انجام می‌شود. QR دوطرفه مخصوص Android و Windows را عمداً به این مرحله وابسته نکرده‌ام، چون پلاگین‌های رایج QR دوربین پشتیبانی یکسانی برای Windows ندارند. در مرحله بعد می‌توان QR نمایش‌داده‌شده را برای Android و اسکن وب‌کم Windows به صورت جداگانه اضافه کرد.
